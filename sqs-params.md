
## Параметры SQS

| Параметр | Описание |
|-----------|-----------|
| **Visibility Timeout** | Период времени, в течение которого сообщение **недоступно другим консюмерам** после того, как один из них его получил. За это время консюмер должен обработать сообщение и удалить его из очереди. Если он этого не делает — по истечении таймаута сообщение снова становится доступно для получения другими консюмерами. |
| **Message Retention Period** | Время, в течение которого сообщение хранится в очереди, если оно **не было успешно обработано** и удалено. По истечении этого срока сообщение **удаляется навсегда**. В DLQ оно попадает только при превышении лимита повторных доставок (`maxReceiveCount`). |
| **Delivery Delay** | Позволяет **задержать доставку** всех новых сообщений на заданный интервал (до 15 минут). Полезно для отложенной обработки или оркестрации зависимостей. |
| **Receive Message Wait Time (Long Polling)** | Максимальное время ожидания (до 20 секунд), которое консюмер будет держать соединение открытым, если очередь пуста. Позволяет снизить нагрузку и затраты, уменьшая количество пустых ответов при опросе очереди. |
| **Maximum Message Size** | Максимальный размер одного сообщения — **256 KB**. Для передачи больших данных рекомендуется использовать **S3 + ссылку** в теле сообщения. |
| **Redrive Policy (DLQ)** | Определяет связанную **Dead Letter Queue** и политику переноса сообщений, которые не удалось обработать после заданного количества попыток (`maxReceiveCount`). |
| **maxReceiveCount** | Количество раз, которое сообщение может быть доставлено консюмеру, прежде чем оно будет перемещено в **Dead Letter Queue**. Например, если `maxReceiveCount = 5`, то после 5 неудачных обработок сообщение попадёт в DLQ. |

---

💡 **Совет:**  
Корректно подбирай связку `Visibility Timeout` и `maxReceiveCount`:  
если таймаут слишком короткий — консюмер не успеет удалить сообщение и оно уйдёт в DLQ по ошибке;  
если слишком длинный — очередь будет “зависать” при сбоях.
